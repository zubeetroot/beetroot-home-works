
def word count
    count = ()
    words = str.split()
        if word in count
            counts[word] += 1
        else:
            counts[word] = 1
        return counts

print( word_count('To już ten moment. Czas na otwarcie igrzysk olimpijskich'))



print(matrix[0])
print(matrix[1])
print(matrix[2])
#method one for showing matrix
print([[row [i] for row in matrix] for i in range(4)])

#method two for showing matrix
print([[row [i] for row in matrix] for i in range(len(matrix[0]))])

#method three for showing matrix
#and if one column is a little shorter then it can't be index 4 - then you have to find the smallest element in the matrix and specify (it
columns = len(min(*matrix, key=len))
print(columns)

[[row [i] for row in matrix] for i in range(4)]

list1 = (1, 2, 3, 4, 5)


print[list1.min()]